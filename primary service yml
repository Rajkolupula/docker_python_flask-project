apiVersion: v1
kind: Service
metadata:
  name: python-app-primary
  annotations:
    harness.io/primary-service: "true"
spec:
  selector:
    app: python-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  type: ClusterIP

